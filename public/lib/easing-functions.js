// ESM shim over the CJS `easing-functions` package (mirrors the jsdelivr
// `+esm` wrapper the browser importmap uses).
import Easing from '../../node_modules/easing-functions/index.js';
export default Easing;
